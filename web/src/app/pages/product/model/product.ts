export interface Product {
    id: string,
    name: String,
    price: Number,
    quantity: Number,
    description: String,
    active: Boolean,
    category: string,
    expirationDate: Date,    
    image: String,
    note: String
}